#include <stdio.h>
/**
  * main - main is entry point
  * Description: 'print single digit of base 10'
  * Return: 0 Success
  */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
